kind: pipeline
type: docker 
name: deploy_dev
trigger:
  branch:
  - main

node:
  environment: ryali

volumes:
  - name: bonfire
    host:
      path: /bonfire

steps:
- name: build
  image: node:latest
  volumes:
    - name: bonfire
      path: /bonfire
  commands:
    - npm install
    - npm run build
    - rm -rf /bonfire/*
    - mv ./build/* /bonfire/
    
