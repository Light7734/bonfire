kind: pipeline
type: docker 
name: deploy
trigger:
  branch:
  - main

steps:
  - name: build
    privileged: true
    image: node:latest
    commands:
      - npm install
      - npm run build

  - name: build
    image: byteflair/scp
    commands: 
      - scp -r ./build light@5.75.206.84:/home/light/bonfire_dev
