kind: pipeline
type: docker 
name: deploy_dev
trigger:
  branch:
  - main

node:
  environment: ryali

steps:
- name: build
  image: node:latest
  commands:
    - npm install
    - npm run build
    - rm -rf /bonfire/*
    - mv ./build/* /bonfire/
    
